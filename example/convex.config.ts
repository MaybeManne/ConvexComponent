import { defineApp } from "convex/server";

/**
 * Example App Convex Configuration
 *
 * This app provides durable browser automation capabilities
 * using Browser Use Cloud.
 *
 * All functions are defined directly in the convex/ folder.
 */
const app = defineApp();

export default app;
